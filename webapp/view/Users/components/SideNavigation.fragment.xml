<SideNavigation
    id="IdSideNavigation1Users"
    xmlns="sap.tnt"
    expanded="false"
    selectedKey="{usersNavModel>/selectedKey}"
    itemSelect="onItemSelect"
>
    <NavigationList id="IdNavigationList1Users" items="{path: 'usersNavModel>/navigation'}">
        <NavigationListItem
            id="IdNavigationListItem1Users"
            text="{usersNavModel>title}"
            icon="{usersNavModel>icon}"
            key="{usersNavModel>key}"
        />
    </NavigationList>
</SideNavigation>
